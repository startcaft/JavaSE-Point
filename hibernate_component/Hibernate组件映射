Hibernate组件的映射

映射步骤：

Ⅰ，添加一个组件类，组件并不是一个实体(没有OID)，所以无需为其添加注解；

Ⅱ，在要引用组件的实体类中添加一个组件类的引用，在get方法使用：
	@Embedded注解即可(注解用于映射组件，表示嵌入对象的映射);
	
	Embedded英文是嵌入式的意思
	
	
 ***component(组件)，是某个实体的逻辑组成部分；
 	1，它与实体的根本区别是没有oid；
 	2，采用component硬是的好处在于，它实现了对象模型的细粒度划分，层次会更加分明，复用率会更高。